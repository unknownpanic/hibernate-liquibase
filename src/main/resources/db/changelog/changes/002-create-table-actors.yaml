databaseChangeLog:
  - changeSet:
      id: create-table-actors
      author: maxima
      changes:
        - createTable:
            tableName: actors
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: name
                  type: varchar(255)
              - column:
                  name: country_id
                  type: bigint
                  constraints:
                    references: countries(id)
                    foreignKeyName: fk_actors_country